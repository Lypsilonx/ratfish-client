import 'package:flutter/material.dart';

class RatfishLogo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Text(
      '🐀🐟',
      style: TextStyle(
        fontSize: 30,
        fontWeight: FontWeight.bold,
      ),
    );
  }
}
